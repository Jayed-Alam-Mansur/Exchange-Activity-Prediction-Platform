# -----------------------------------------------------------------------------
# AIESEC Exchange Activity Prediction Platform
# Python >= 3.10
# -----------------------------------------------------------------------------

# Core data stack
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scipy>=1.10

# Configuration & secrets
PyYAML>=6.0
python-dotenv>=1.0

# HTTP / API
requests>=2.31
urllib3>=2.0

# Machine learning
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# Time series
statsmodels>=0.14

# Visualization
plotly>=5.18
matplotlib>=3.7
seaborn>=0.13

# Dashboard
streamlit>=1.30

# Testing
pytest>=7.4

# -----------------------------------------------------------------------------
# OPTIONAL: Prophet.
# Not installed by default because it pulls a heavy cmdstan/pystan toolchain and
# frequently fails on clean CI images. The modelling suite detects its absence
# and skips the Prophet candidate with a warning - nothing else breaks.
#   pip install prophet
# -----------------------------------------------------------------------------
# prophet>=1.1
